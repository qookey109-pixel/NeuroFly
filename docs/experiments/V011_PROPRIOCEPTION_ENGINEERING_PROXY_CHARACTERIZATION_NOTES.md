# Notes

The fixed engineering sweep is a regression test, not a biological experiment. No result from this stage may be used to infer SNpp identity, biological polarity, membrane current, synaptic weight, timing kernel, or behavioral phase.

The stage remains compatible with the unresolved polarity freeze because it operates entirely on systematic-type-agnostic engineering channels.
