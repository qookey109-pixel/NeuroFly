name: NeuroFly Memory VG Delay Moderator Audit

on:
  workflow_dispatch:
  push:
    branches:
      - research/v1-memory-vg-delay-moderator-audit
    paths:
      - "data/memory_vg_delay_moderator_audit_v01.json"
      - "src/neurofly/memory_vg_delay_moderator_audit.py"
      - "tests/test_memory_vg_delay_moderator_audit.py"
      - "docs/experiments/V1_MEMORY_VG_DELAY_MODERATOR_AUDIT.md"
      - ".github/workflows/memory-vg-delay-moderator-audit.yml"

permissions:
  contents: read
  actions: read

concurrency:
  group: neurofly-memory-vg-delay-moderator-audit
  cancel-in-progress: true

jobs:
  memory-vg-delay-moderator-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      STONKFLY_DATA: /home/runner/.cache/neurofly/stonkfly
      SOURCE_STATE: runs/free-malecns
      STUDY_DIR: runs/memory-vg-delay-moderator-audit
      BASE_CHECKPOINT: runs/memory-vg-delay-moderator-audit/base-brain.npz
      RECEIPT: runs/memory-vg-delay-moderator-audit/memory-vg-delay-moderator-audit-receipt.json

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Restore prepared MaleCNS cache
        uses: actions/cache/restore@v4
        with:
          path: /home/runner/.cache/neurofly/stonkfly
          key: malecns-v1-${{ hashFiles('pyproject.toml') }}

      - name: Restore latest production checkpoint read-only source
        uses: actions/cache/restore@v4
        with:
          path: runs/free-malecns
          key: neurofly-v06-state-memory-vg-delay-moderator-audit-${{ github.run_id }}
          restore-keys: |
            neurofly-v06-state-

      - name: Install pinned Stonkfly integration
        run: python -m pip install -e '.[test,stonkfly]'

      - name: Validate preregistered discovery contract
        run: pytest -q tests/test_memory_vg_delay_moderator_audit.py

      - name: Prepare and verify MaleCNS
        run: |
          mkdir -p "$STONKFLY_DATA" "$STUDY_DIR"
          python -m stonkfly prepare
          python -m neurofly brain-status
          python -m neurofly host-preflight | tee "$STUDY_DIR/preflight.json"

      - name: Copy production brain into isolated baseline
        run: |
          test -f "$SOURCE_STATE/brain.npz"
          cp "$SOURCE_STATE/brain.npz" "$BASE_CHECKPOINT"
          test "$(realpath "$SOURCE_STATE/brain.npz")" != "$(realpath "$BASE_CHECKPOINT")"
          sha256sum "$SOURCE_STATE/brain.npz" "$BASE_CHECKPOINT"

      - name: Select histories, freeze moderator features, then execute effect audit
        run: |
          python -m neurofly.memory_vg_delay_moderator_audit \
            --base-checkpoint "$BASE_CHECKPOINT" \
            --output-dir "$STUDY_DIR" \
            --receipt "$RECEIPT"

      - name: Validate discovery evidence without opening claims
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path("runs/memory-vg-delay-moderator-audit/memory-vg-delay-moderator-audit-receipt.json")
          r=json.loads(p.read_text())
          assert r["schema"]=="neurofly-memory-vg-delay-moderator-audit-receipt-v0.1"
          assert r["status"]=="EXPLORATORY_MEMORY_VG_DELAY_MODERATOR_AUDIT_COMPLETE"
          assert r["execution_valid"] is True
          assert all(r["config_gates"].values())
          assert all(r["evidence_gates"].values())
          selected=r["selection_manifest"]["selected"]
          assert len(selected)==8
          assert len({x["pre_event_checkpoint_sha256"] for x in selected})==8
          assert len({x["stable_sensory_sequence_sha256"] for x in selected})==8
          fm=r["pre_intervention_feature_manifest"]
          assert fm["effect_endpoints_evaluated_before_manifest"] is False
          assert len(fm["rows"])==8
          assert r["aggregate"]["moderator_analysis"]["feature_ranking_or_threshold_applied"] is False
          assert r["aggregate"]["moderator_analysis"]["exploratory_only"] is True
          for key in (
              "learning_validated","temporal_cue_index_confirmed","prefix_sequence_specificity_confirmed",
              "transient_state_carrier_confirmed","memory_expression_causal","moderator_confirmed",
              "replacement_confirmatory_authorized","behavioral_promotion_authorized",
          ):
              assert r[key] is False
          assert r["production_checkpoint_mutated"] is False
          print("MEMORY_VG_DELAY_MODERATOR_AUDIT_VALID",r["receipt_sha256"],r["aggregate"]["moderator_analysis"]["label_counts"])
          PY

      - name: Publish evidence only
        uses: actions/upload-artifact@v4
        with:
          name: neurofly-memory-vg-delay-moderator-audit-${{ github.run_id }}
          path: |
            runs/memory-vg-delay-moderator-audit/preflight.json
            runs/memory-vg-delay-moderator-audit/selected-neural-histories.json
            runs/memory-vg-delay-moderator-audit/pre-intervention-features.json
            runs/memory-vg-delay-moderator-audit/memory-vg-delay-moderator-audit-receipt.json
          if-no-files-found: error
          retention-days: 14
