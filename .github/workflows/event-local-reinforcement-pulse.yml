name: NeuroFly Event Local Reinforcement Pulse

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: neurofly-event-local-reinforcement-pulse
  cancel-in-progress: false

jobs:
  event-local-reinforcement-pulse:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      STONKFLY_DATA: /home/runner/.cache/neurofly/stonkfly
      SOURCE_STATE: runs/free-malecns
      STUDY_DIR: runs/event-local-reinforcement-pulse
      BASE_CHECKPOINT: runs/event-local-reinforcement-pulse/base-brain.npz
      RECEIPT: runs/event-local-reinforcement-pulse/event-local-reinforcement-pulse-receipt.json
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Restore prepared MaleCNS cache
        uses: actions/cache/restore@v4
        with:
          path: /home/runner/.cache/neurofly/stonkfly
          key: malecns-v1-${{ hashFiles('pyproject.toml') }}
      - name: Restore latest production checkpoint read-only source
        uses: actions/cache/restore@v4
        with:
          path: runs/free-malecns
          key: neurofly-v06-state-event-local-pulse-${{ github.run_id }}
          restore-keys: |
            neurofly-v06-state-
      - name: Install pinned Stonkfly integration
        run: python -m pip install -e '.[test,stonkfly]'
      - name: Prepare and verify MaleCNS
        run: |
          mkdir -p "$STONKFLY_DATA" "$STUDY_DIR"
          python -m stonkfly prepare
          python -m neurofly brain-status
          python -m neurofly host-preflight | tee "$STUDY_DIR/preflight.json"
      - name: Copy production brain into isolated diagnostic baseline
        run: |
          test -f "$SOURCE_STATE/brain.npz"
          cp "$SOURCE_STATE/brain.npz" "$BASE_CHECKPOINT"
          test "$(realpath "$SOURCE_STATE/brain.npz")" != "$(realpath "$BASE_CHECKPOINT")"
          sha256sum "$SOURCE_STATE/brain.npz" "$BASE_CHECKPOINT"
      - name: Execute event-local reinforcement pulse intervention
        run: |
          python -m neurofly.event_local_reinforcement_pulse             --base-checkpoint "$BASE_CHECKPOINT"             --output-dir "$STUDY_DIR/replicates"             --receipt "$RECEIPT"
      - name: Validate event-local receipt without opening claims
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path("runs/event-local-reinforcement-pulse/event-local-reinforcement-pulse-receipt.json")
          r=json.loads(p.read_text())
          assert r["schema"]=="neurofly-event-local-reinforcement-pulse-receipt-v0.1"
          assert r["status"]=="EXPLORATORY_INTERVENTION_COMPLETE"
          assert r["execution_valid"] is True
          assert all(r["config_gates"].values())
          assert all(r["evidence_gates"].values())
          for k in ("learning_validated","single_event_reinforcement_effect_causal","temporal_credit_defect_confirmed","causal_learning_claim_authorized","replacement_confirmatory_authorized","behavioral_promotion_authorized"):
              assert r[k] is False
          print("EVENT_LOCAL_REINFORCEMENT_PULSE_VALID",r["receipt_sha256"],r["aggregate"])
          PY
      - name: Publish event-local evidence only
        uses: actions/upload-artifact@v4
        with:
          name: neurofly-event-local-reinforcement-pulse-${{ github.run_id }}
          path: |
            runs/event-local-reinforcement-pulse/preflight.json
            runs/event-local-reinforcement-pulse/event-local-reinforcement-pulse-receipt.json
          if-no-files-found: error
          retention-days: 7
